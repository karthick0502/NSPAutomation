Optional: add a PNG or JPG sketch here and run:

  python run.py sample_emails/enquiry_01.txt --image sample_attachments/your_sketch.png

The model will use vision to summarise what the drawing shows (layout, labels) and merge that with the email text.
