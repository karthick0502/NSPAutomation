# Copy to .env and fill in one provider.

# OpenAI (recommended for structured JSON + optional vision on attachments)
OPENAI_API_KEY=sk-...

# Model override (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Logging: DEBUG, INFO, WARNING, ERROR (CLI and server)
LOG_LEVEL=INFO
